# SKIPGEO 智控台 V4.1 升级实施计划（当前版本）
## 多项目资产化 + GEO 发展曲线 + 引用诊断闭环

**依据**：三轮方案评审（多项目/快照/曲线 → 生产化缺口修正 → 全球对标 Profound/MaxAEO + 可行性核验）的合并结论
**创建**：2026-09-04 · **v4.1 自审修订**：2026-09-04（9 处，详见文末修订记录）
**工期估计**：约 7.5 个工作日，分四期交付（一期拆 1a/1b 两批发布），每期独立可用、独立验收
**版本**：v4.1
**代码基线**：system/ V3.3（server.py 417 行 / app.js 713 / ops.js 437 / data.js 393 / chat.js 116；test_e2e.py 13/13 通过；anysearch 已修复并实测可用）

---

## 0. 目标与产品原则

**一句话目标**：把智控台从"单园区监测台账"升级为"多园区数字资产 + 诊断闭环系统"——项目即资产、曲线自动生成、监测数据能直接变成修复路线图。

**产品原则（实现时优先级高于任何单项功能）**：
1. **≤20 分钟采集纪律**：人工监测每轮成本必须 ≤20 分钟，否则系统会饿死。全量 180 条只在季度做。
2. **统计诚实**：曲线点必带样本量 n；n<10 的点不进主曲线；竞品做参照线；部署事件后标注预期窗口（首批引用 10–15 天 / 稳定 8–12 周），防过早判"无效"。
3. **数据先于视图**：先持久化探针全量结果（现状只存 3 域名/问，70% 数据被丢弃），再建聚合视图。
4. **零依赖不变**：不引入任何前端框架/图表库，延续纯原生 JS + 标准库 Python。

**明确不做（边界）**：
- 六大中文引擎答案层全自动化——无公开 API，行业现实，不追（搜索通道代理 + 人工抽样 + Ollama 解析是当前最优解）
- 多租户鉴权——内网信任假设，写进 README 边界声明；操作留痕部分补偿
- 实时 AI 爬虫分析（Profound Agent Analytics 等价物）——需要服务端埋点权限，远超当前场景

---

## 1. 已确认的三个数据缺陷（本次修复的对象）

| # | 缺陷 | 代码位置 | 后果 |
|---|---|---|---|
| 1 | 单项目单文档：`kv` 表只有一个 `doc` 键存全部状态 | server.py `kv_put("doc",...)` | 多园区数据互相污染 |
| 2 | 监测工具硬编码：30 问、口径种子全写死且为蛇口网谷专属 | data.js `GEO.prompts` / `GEO.caliberSeed` | 第二个园区量错尺子 |
| 3 | 探针结果用完即弃：top-10 只存前 3 域名进 note 字符串 | ops.js:280-288 | 引用诊断缺数据基础 |

另有：server.py:32 `OWN_DOMAINS` 硬编码招商域名，换项目后自有阵地判定全错。

---

## 2. 目标数据模型与 API

### 2.1 SQLite（kv 表新增键）

```
"projects"            → [{id, name, url, brand, ownDomains[], createdAt, archived, lastActive}]
"doc:project:<id>"    → {rev, state}          # 每项目独立乐观锁（沿用现有 409 协议）
"audit"               → [{ts, actor, project, action, detail}]   # 操作留痕，上限 500 条滚动
迁移：旧 "doc" 键 → 自动创建默认项目「蛇口网谷」，state 原样迁入，迁完保留旧键 30 天
备份：backups/geodesk-YYYYMMDD.json（全部项目导出，启动时 + 每日定时，保留最近 30 份）
```

### 2.2 项目 state 扩展字段（新增，旧字段不动）

```
prompts:      [{id, cat, q, severity(1-5)}]                        # 项目化 30 问，severity 可编辑
                                                                 # 种子策略（建项目时默认）：对比竞品类=5、选址决策=4、品牌认知=3、产业服务=3，可改
ledger 记录扩展: cooccur[]                                          # 本条答案中出现的竞品名列表（参照线与共现分析的数据落点；
                                                                 # 1.6 批量粘贴与 3.4 解析器自动填充，人工可改）
probes:       [{date, promptId, domains[10], urls[10], ownHit}]    # 探针全量持久化（缺陷#3）
snapshots:    [{ts, type(diag|round), auditPct, diagFails, ownHitN, mentionAvg, mentionN, engineN}]
events:       [{date, type(deploy|wo|engine|custom), label}]       # 曲线事件标注（人工+自动）
evidence:     [{id, type(引言|案例|荣誉|口径引用), content, person, title, source, asOf, status(待采集|已核验)}]  # 叙述性证据；数字事实唯一源仍是口径表
watchPages:   [{name, url, promptIds[], deployedAt, hits:[{date, seen, where}]}]   # 资产追踪
business:     [{month, aiVisits, aiLeads, note}]                   # 业务结果月度登记
remediation:  扩展 {owner, due, priority, linkedPrompt, verified, verifiedNote}    # 工单升级
operator:     "操作员名"                                            # 留痕用，顶部可设置
```

### 2.3 API 变更（server.py）

```
GET  /api/projects                          → 项目列表（含汇总指标：体检分/提及率/最近活动）
POST /api/projects  {name,url,brand,ownDomains}  → 新建 → id
GET  /api/data?project=<id>                 → {state, _rev}
PUT  /api/data  {project, base_rev, state}  → 每项目独立 409 冲突协议
POST /api/probe    {query, ownDomains?}     → own 判定参数化（替换硬编码）
POST /api/diagnose {url, brand, ownDomains?}
POST /api/parse    {text, brand, competitors[]} → {mention, sentiment, citedDomains[], competitorMentions[]}
                                              # Ollama 结构化抽取（复用 pick_chat_model；JSON 模式输出；
                                              # 失败返回 503，前端回退人工填写）
```

---

## 3. 分期任务与验收标准

### 第一期 · P0 数据基座（约 2–2.5 天，拆 1a/1b 两批发布）
**目标**：多项目隔离 + 监测工具项目化 + 探针数据不再丢失 + 采集降本到位。完成后即可接入第二个园区。
**拆分**：1a = 1.1+1.2+1.8（项目层+重构+测试，回归风险集中区，单独发布试运行）；1b = 1.3–1.7（在 1a 验证稳定后并入）。

| 任务 | 内容 | 文件 | 难度 |
|---|---|---|---|
| 1.1 服务端项目层 | projects 键 + doc:project:\<id\> + 旧 doc 迁移 + API 改造（2.3） + 每日备份线程 | server.py | 中 |
| 1.2 前端 state 重构 | 全局 `state` → `S()` getter 取当前项目（调用点最小改动）；localStorage 键 `geodesk.v3.<projectId>`；**本地模式（双击 index.html）同样支持多项目：项目列表存 localStorage `geodesk.projects`，首次打开把旧本地数据自动迁入默认项目**；mergeServerState 按项目生效；顶栏项目切换器 + 新建项目对话框（名称/域名/品牌词/自有域名）；**chat.js 适配：/api/chat 请求带 project 参数，助手上下文 state_summary 按项目读取**（否则内置助手在新架构下失明） | app.js, index.html, chat.js, server.py | 中（回归风险最高） |
| 1.3 监测工具项目化 | 建项目时 prompts 从 GEO.prompts 种子化进 state（含 severity 字段），30 问管理页可增删改；caliberSeed 同理；所有读 GEO.prompts 的调用点改读 S().prompts | data.js, app.js, ops.js | 中 |
| 1.4 探针全量持久化 | 批量跑时把每问 top-10 域名+URL 存入 state.probes（修复缺陷#3）；历史 note 字符串里的 3 域名做一次性解析回填 | ops.js | 低 |
| 1.5 自有域名参数化 | diagnose/probe 请求体带项目 ownDomains，替换 server.py 硬编码 | server.py, ops.js, app.js | 低 |
| 1.6 批量粘贴录入 | 台账页新增"粘贴批量录入"：textarea 粘 TSV/CSV（列序固定+表头识别）→ 预览确认 → 查重提示 → 入账 | app.js, index.html | 低 |
| 1.7 分层监测（采样模式） | 监测页新增"本轮人工采样"卡片：自动生成"本轮该测的 10 问 × 2 引擎"（severity 降序 + 上轮未测引擎优先轮换），逐条打勾，界面显示"预计 ≤20 分钟"；季度全量提醒（距上次全量 ≥90 天时提示） | app.js, index.html | 中 |
| 1.8 测试与迁移 | test_e2e 扩展（见 §5）；迁移前自动备份 db | test_e2e.py | 中 |

**验收标准（全部满足才算过）**：
- [ ] 新建项目 B 后，A/B 的台账、体检、口径表、诊断历史完全隔离（e2e 用例验证）
- [ ] 旧"蛇口网谷"数据迁移后原样可见，13 项旧 e2e 全部继续通过
- [ ] 跑一轮 30 问后 state.probes 有 30 条 × 10 域名（不再是 3 个）
- [ ] 项目 B 填入自己的域名后，一键诊断的品牌词自有阵地判定用 B 的域名
- [ ] 粘贴 20 行 TSV → 预览 → 20 条入账，重复行被标记
- [ ] 采样卡片一轮 ≤20 条、显示预计耗时徽标
- [ ] 服务器连续运行一天后 backups/ 下有当日 JSON

### 第二期 · P1 曲线与资产（约 1.5 天）
**目标**：GEO 发展曲线自动生成，"效果怎么看"有答案；资产与数据可找回。

| 任务 | 内容 | 文件 | 难度 |
|---|---|---|---|
| 2.1 快照机制 | 一键诊断完成 / 每轮监测（自动+人工）完成时自动写 snapshot（含 mentionN 样本量）；首次快照锁定为基线 | ops.js | 低 |
| 2.2 曲线页（监测→"发展曲线"区） | 三线同屏：体检成熟度(0-100)/提及率(%)/品牌词自有阵地数(0-10)；**通道分层（口径红线）：信源侧（搜索通道自动轮）与答案侧（六引擎人工轮）的提及率必须分线显示或可切换，禁止混在一条线里平均——两者是不同概念的指标**；**竞品共现提及率灰色参照线（数据来源：ledger.cooccur[]）**；事件标注竖线（部署/工单验收/引擎规则变动，来自 events）；预期窗口带（部署事件后 10–15 天、8–12 周浅色区）；3 点移动平均；悬浮显示 n；n<10 的点降权显示 | app.js（扩展 trendSvg） | 中 |
| 2.3 基线对比卡 | 工作台三个 KPI 卡改为"基线 → 当前 + Δ"（如 提及率 12%→27% +15pp），无基线时引导先跑基线 | app.js | 低 |
| 2.4 资产追踪 | 提升包下载时引导登记物料 URL 进 watchPages；"查进榜"按钮：**优先复用最近 ≤7 天的 probes 数据，仅对缺失/过期的关联 prompts 补跑探针**（避免每次全量外呼），命中则记 hits 时间线 | ops.js, app.js | 中 |
| 2.5 备份与留痕 | 每日备份落盘（1.1 已含服务端）；**操作留痕由 server 侧自动记录：PUT/关键 API 请求体携带 operator 字段，server 追加写 audit 键（前端不直接写 audit，避免多项目并发整键覆盖丢数据）**；**备份导入端点 POST /api/restore（接收备份 JSON 重建 projects + doc:project:\<id\>，需二次确认）**——否则"可恢复"只停留在文档；设置页可查最近 100 条留痕 | server.py, app.js | 低 |

**验收标准**：
- [ ] 完成 ≥2 次诊断/监测轮后曲线自动出现，基线点有标记
- [ ] 曲线上能看到：三线 + 参照线 + 事件竖线 + 预期窗口带；悬浮显示样本量
- [ ] 工作台 KPI 显示 Δ 且与快照数据一致
- [ ] 物料登记后"查进榜"能跑探针并记录命中时间线
- [ ] 任意写操作后操作留痕可查；备份文件可被手动导入恢复（e2e 验证恢复路径）

### 第三期 · P1+ 诊断闭环（约 2 天）
**目标**：监测数据 → 修复路线图 → 派单 → 验证，闭环打通；内容生产接入证据层。

| 任务 | 内容 | 文件 | 难度 |
|---|---|---|---|
| 3.1 引用诊断视图（监测→"谁在赢"区） | ① 域名榜：聚合 probes+台账 url → 我们的 30 问被哪些域名赢走（次数/趋势），自有域名高亮，**按 data.js 已有知识标注域名类型（通吃平台/基础层/其他），"被 CSDN 赢"与"被百科赢"策略含义不同**；② 逐问下钻：点任一问看它历次 top-10 变化；③ 诊断矩阵：提及未引用/竞品被引（答案侧·台账）/自有页在榜未被引（信源侧·probes）等状态分类列表，**每类标签标明数据通道来源**，每类挂"生成工单"按钮 | app.js | 中 |
| 3.2 工单升级 | remediation 增 owner/期限/优先级（公式 = 业务价值×可见度缺口×可修复性×准确性风险÷工作量，各 1–5 分下拉自动算）/关联 prompt/部署后验证勾选（"已确认被收录/被引用"）；工单列表按优先级排序 | ops.js, app.js | 低 |
| 3.3 证据库 | 诊断→"证据库"：引言/案例/荣誉 CRUD（人物/职务/来源/时点/核验状态）；**边界（口径一致性红线）：证据库只存叙述性证据（引言/案例/荣誉），数字类事实一律引导入口径表——证据库"数据"类型仅作为口径表条目的引用指针，不另立数值，避免出现两个事实源打架**；一园一档与 FAQ 生成器从证据库取材（引言占位符自动填充已核验条目）；90 天路线图的"每园区 ≥5 条引言"进度条 | app.js, ops.js | 中 |
| 3.4 Ollama 答案解析器 | server 端 /api/parse（2.3 规格）；台账录入页加"贴答案自动填"：粘 AI 回答原文 → 自动填 提及/情感/引用域名/**竞品共现（写入 cooccur）** → 人确认后入库；模型不可用时优雅降级为纯手工；**验收前置条件：实施前先收集 10 个真实 AI 回答样本（豆包/DeepSeek/Kimi 各若干）作为抽取质量测试集** | server.py, app.js, chat.js | 中 |

**验收标准**：
- [ ] 域名榜基于真实 probes 数据渲染，点问题可下钻看历史变化
- [ ] 诊断矩阵至少支持"提及未引用""竞品被引"两类，可一键生成带上下文的工单
- [ ] 工单可填 owner/期限，优先级由公式自动计算并排序
- [ ] 证据库录入 3 条引言后，重新生成一园一档时引言段落自动填充
- [ ] /api/parse 用 10 个真实 AI 回答抽查：≥80% 字段抽取正确，且全部经人工确认才入库（不自动提交）

### 第四期 · P2 规模化（约 1.5 天）
**目标**：集团视角 + 归因深化 + 业务连接。

| 任务 | 内容 | 难度 |
|---|---|---|
| 4.1 组合对比视图 | 工作台顶部"全部项目"视图：每项目 体检分/提及率/趋势箭头/最近活动时间/落后项预警，按提及率变化排序，支持归档 | 中 |
| 4.2 引擎分层与引用份额曲线 | 曲线页加引擎筛选（单引擎曲线 vs 总均线对照）；引用份额曲线（台账 url 中自有域名占比按期） | 低 |
| 4.3 业务结果连接 | 监测页"业务结果"月度表单：AI 引荐流量（GA 口径）/AI 来源线索数/备注；随快照进曲线副轴；附"招商联系表单加'从哪个AI了解到我们'字段"的实施建议模板（交付给 IT 的文案） | 低 |
| 4.4 严重度公式化 | prompt 编辑页支持公式辅助打分（买家接近度+竞品替代+准确性风险+营收相关性，各 1–5） | 低 |
| 4.5（可选）探针回退 | run_probe 失败时尝试 mmx 作为第二通道（保持证据标注来源引擎） | 低 |

**验收标准**：
- [ ] 组合视图正确聚合多项目且与单项目页数据一致
- [ ] 引擎筛选后曲线与该引擎台账数据吻合
- [ ] 业务表单录入后曲线副轴可见；月度对比可导出 CSV

---

## 4. 任务依赖与执行顺序

```
1.1 → 1.2 → 1.3/1.4/1.5（并行）→ 1.6/1.7（并行）→ 1.8 ── 一期验收
一期 → 2.1 → 2.2/2.3（并行）；2.4 依赖 1.4；2.5 独立 ── 二期验收
二期 → 3.1（依赖 1.4 probes）；3.2/3.3 独立可并行；3.4 独立 ── 三期验收
三期 → 4.x 全部独立 ── 四期验收
```

---

## 5. 测试策略（test_e2e.py 扩展清单）

保留现有 13 项全绿为硬门槛，新增：
1. 多项目隔离：建 A/B 两项目，A 写台账 → B 读不到；切换后各自数据完整
2. 迁移：用带旧 doc 键的测试 db 启动 → 自动生成默认项目且数据一致，旧键保留
3. 并发：两路 PUT 同一项目不同 base_rev → 后者收 409 + 最新 state
4. probes 持久化：mock 探针跑一轮 → state.probes 30×10
5. ownDomains 参数化：同一 URL 在两个项目的 own 判定结果不同
6. 批量粘贴：TSV 解析 + 查重用例
7. 快照与曲线：造 ≥3 个快照点 → 曲线 SVG 含基线标记与参照线节点
8. 备份恢复：生成备份 JSON → 新 db 导入 → 数据一致
9. /api/parse：Ollama 不可用时返回 503（优雅降级）；可用时返回合法 JSON
10. 无头 Chrome 渲染：新页面（曲线/诊断/组合/证据库）在 1280 与 375 宽度无横向滚动、无 JS 报错

---

## 6. 上线与回滚

- 每期开发前 `cp system/geodesk.db system/geodesk.db.bak-<日期>`；backups/ 目录成为常态保障
- 服务端向前兼容：新增字段旧前端忽略不报错；新前端读旧数据自动迁移（迁移代码幂等，可重复执行）
- 上线节奏：一期后试运行 ≥3 天（真实跑一轮 30 问 + 第二园区接入）再进二期；后续每期间隔 ≥1 天观察
- 回滚：恢复 db 备份 + 还原 system/ 旧版文件即可，无外部依赖需要清理

---

## 7. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| 多项目 state 重构引入回归 | 高（工程） | `S()` getter 最小化改动；13+10 项 e2e 全绿门槛；db 预先备份 |
| 采集纪律失败（没人录数据） | 高（运营） | ≤20 分钟规则产品化（1.7）；解析器降本（3.4）；自动轮零人力兜底 |
| anysearch 再次不稳定 | 中 | 已修复（urllib 补丁）；失败如实报错保留；4.5 mmx 回退可选 |
| localStorage 容量（多项目长期累积） | 中 | 每项目独立键；项目归档导出 JSON 后可清本地 |
| Ollama 未运行 | 低 | 解析器 503 优雅降级为手工填写，主流程不受影响 |
| 证据库含内部引言存共享服务器 | 低 | 内网信任边界声明；鉴权明确列入"不做"，README 标注 |

---

## 8. 前端设计资源推荐（按全局规则一，三类来源均已读取）

本次为**既有成熟系统的扩展**，设计连续性优先于新设计语言。推荐组合：

| 来源 | 资源 | 用途 |
|---|---|---|
| ZCode skill | `web-design-guidelines` | 新增页面双端核查（规则二：桌面 1280/1440/1920 多栏、移动 320/375/414/768 单栏、触控 ≥44px、不依赖 hover） |
| ZCode skill | `hallmark` | 新页面去 AI 味审计（规则三：不用模板化三卡片结构、不造数据、延续招商蓝/金 #014070/#AA9168 真实色板） |
| 插件 | `browser-use:web-gui-tester`（或沿用 test_e2e 的无头 Chrome） | 新视图真实渲染验证 |
| Open Design MCP（已调 list_skills 读取） | `design-review`（可选终审）；`d3-visualization` **明确不用**（避免引入图表库依赖，延续自研 SVG） | 上线前视觉审计 |

**设计决策差异说明（规则二要求）**：曲线页桌面端三线+参照线同屏、悬浮看样本量；移动端默认只显示主曲线+最新值、点击切线，参照线折叠为图例——避免小屏信息过载。诊断视图桌面端左榜右钻取双栏，移动端上下堆叠。

---

## 9. 交付物清单（全部完成后）

1. system/ V4.0（四期全部功能）+ 扩展后的 test_e2e.py（≥23 项全绿）
2. 本计划勾选后的验收记录
3. README 更新：V4.0 能力说明、多项目使用指南、"能力边界"更新（含鉴权边界声明）
4. 交付 IT 的两个模板：招商表单"AI 来源"字段文案、AI 引荐流量 GA 查看路径说明

---

## 修订记录

**v4.1（2026-09-04 · PM+GEO 专家自审）**：自审发现并修订 9 处——
1. **（高）竞品参照线无数据落点**：ledger 记录新增 `cooccur[]` 字段（原计划参照线引用的竞品共现数据在台账 schema 里不存在，功能做不出来）；1.6/3.4 负责填充。
2. **（高）chat.js 内置助手遗漏适配**：项目化后 state_summary 读不到项目数据，助手失明；纳入 1.2。
3. **（口径）曲线通道分层红线**：搜索通道（信源侧）与六引擎（答案侧）提及率禁止混均一条线；写入 2.2。
4. **（协议）操作留痕改 server 侧记录**：原设计前端直写 audit 键，多项目并发会整键覆盖丢数据；改 PUT/关键 API 由 server 追加；写入 2.5。
5. **（一致性）验收要求了不存在的功能**：备份"可导入恢复"补 POST /api/restore 任务；写入 2.5。
6. （中）severity 种子策略缺失：1.7 采样排序依赖 severity，建项目时按问题类别给默认种子（对比竞品 5/选址 4/品牌 3/服务 3）；写入 §2.2 与 1.3。
7. （中）本地模式（双击 index.html）多项目迁移路径未定义：项目列表存 localStorage，首开自动迁移旧数据；写入 1.2。
8. （中）证据库与口径表双事实源风险：证据库收窄为叙述性证据+口径引用指针，数字事实唯一源仍是口径表；改 3.3 与 §2.2。
9. （小）watchPages"查进榜"改为复用 ≤7 天内 probes 再补跑，避免重复外呼；一期拆 1a/1b 降大爆炸风险，工期 7→7.5 天。

**v4.1-1a 实施记录（2026-09-04 · 已完成）**：
- 服务端：项目层（`projects` 键 + `doc:project:<id>` + 幂等迁移，旧键保留）、GET/POST `/api/projects`、`/api/data` 项目级乐观锁、`/api/probe|/api/diagnose` ownDomains 参数化、`/api/audit` server 侧留痕、`/api/backup` + 启动/每日备份线程（backups/ 保留 30 份）、chat 项目感知。
- 前端：顶栏项目切换器 + 新建项目弹窗（名称/域名/品牌词/自有域名）、localStorage 分项目键 + 本地模式首开迁移、`initProjects()` 模块级装载（node harness 兼容）、冲突合并键补 prompts/diagHistory、ops.js 诊断与批量跑传当前项目域名、chat.js 带 project 参数。
- 测试：test_e2e 32 项全绿（13 旧 + 19 新：迁移/隔离/项目级409/404/备份/留痕/前端项目栏）。
- 真实环境验证：真实 db 迁移完整（30 台账/5 口径/3 诊断史/rev9）；ownDomains 参数化生效（百科→1，默认→0 与基线一致）；无头 Chrome 1280/375 双端渲染通过；chat 助手读出项目真实数据；system/backups/geodesk-20260904.json 已生成。
- 遗留到 1b：新建项目交互式浏览器全流程测试（browser-use）、probes 持久化（1.4）、批量粘贴（1.6）、采样模式（1.7）、监测工具项目化读点切换（1.3）。

**v4.1-1b 实施记录（2026-09-04 · 已完成）**：
- 1.3 监测工具项目化：读点全部切至 `P_prompts()/P_cats()`（自愈拷贝，编辑不污染全局模板）；监测页「管理问题矩阵」（编辑/增删/严重度/批量替换换园区名）；新建项目不带蛇口网谷口径种子。
- 1.4 探针全量持久化：批量跑写入 `state.probes`（每问 top-10 域名+URL+ownHit）；历史 note 前3域名一次性回填（标志位+日期×问题ID 双去重）。
- 1.6 批量粘贴：`parseBatchRows` 纯函数（TSV/CSV、表头跳过、词/数字双口径、坏 ID 拒绝）→ 预览（重复标记）→ 导入。
- 1.7 采样模式：`pickSample` 纯函数（严重度降序+覆盖最少优先；引擎取人工记录最少 2 个轮换）；采样卡（复制问题→点引擎chip自动选入②区→今日进度 X/20）；季度全量提醒（>90天）。
- 台账新增 `cooccur[]` 竞品共现（单条录入/批量粘贴/CSV 导出三处支持）——二期竞品参照线的数据落点。
- **交互式验收（隔离实例真实浏览器）发现并修复 3 个同步 bug**：①新建项目后 REV_BASE 未重置（沿用上一项目 rev → 永久 409）；②409 恢复路径 `server.ledger.concat` 对空项目抛 TypeError 被静默吞掉；③新项目缺 `probesBackfilled` 标记导致重载重复回填。修复后数据完整同步服务器（rev=1、27台账、25 完整探针×10域名）。
- 全量回归：**39 项断言，38 通过 1 跳过 0 失败**（跳过项=anysearch 上游 TLS 瞬断，设计内可跳过）。
- 注：项目切换器与「项目总览」页由并行设计会话重构为 proj-chip+项目卡片形态，双方改动已完整整合并回归通过。

**v4.1-二期 实施记录（2026-09-04 · 已完成）**：
- 2.1 快照：`computeSnapshot`（纯函数，通道分层：mentionAns/mentionSrc/ownHit 独立字段）+ `pushSnapshot`（诊断/批量跑/批量导入/单条录入四处钩子；每日每型一条，同日替换；首个快照锁基线）+ `addEvent`。
- 2.2 曲线：`renderCurve` 多序列 SVG——体检成熟度/答案侧提及/信源侧命中/自有阵地/竞品共现率（参照线，默认关）；水平型指标前向填充、期间型指标断线（口径红线落实）；3点移动平均；事件竖线；部署/工单事件的 10–15 天与至 8–12 周预期窗口带（金色）；n<10 空心降权；`<title>` 悬浮显示值与样本量；系列 chips 可切换（移动端默认只开体检+答案侧）；旧 trendSvg（混均折线）废弃。工单验收、资产首次进榜自动记事件；曲线卡带「＋记事件」入口。
- 2.3 基线对比卡：工作台三 KPI = 体检（+N分 vs 基线）/提及率（答案侧优先·标注通道与 n·+Npp）/自有阵地（N/总数·+N问），单快照显「首测即基线」。
- 2.4 资产追踪：提升包单文件下载与打包下载即登记 watchPages（+deploy 事件）；管理卡（补 URL/删/查进榜）；查进榜复用 ≤7 天 probes，结果含最高位次与预期窗口提示，hits 时间线入库，首次进榜自动记事件。
- 2.5 留痕与恢复：`POST /api/restore`（confirm:true 二次确认 + 格式校验）；PUT 携带 operator（项目总览页署名框，localStorage 记忆）；项目总览页新增留痕卡（最近100条）与备份恢复卡。
- **本期交互验收又逮住 2 个 bug 并修复**：①`probesBackfilled` 放在 defaultState 导致老项目跳过历史回填（且旧值已被浏览器同步上服务器）→ 移到 submitNewProject + migrateProbes 自愈（flag=true 但 probes 空且有可回填 note 时照样回填）；②`loadProjectFromServer` 服务器分支不走回填 → 补 migrateProbes 调用。
- 验收（隔离实例真实浏览器）：曲线 SVG（折线/基线/窗口带×4/事件竖线/悬浮 n/5 系列 chips）✓；Δ卡（+4分/+7pp 答案侧 n=14/+2问，通道标注）✓；27 条历史探针自愈回填 ✓；查进榜 0/27 诚实结果+预期窗口提示 ✓；留痕含署名 ✓；恢复 e2e 三项 ✓。
- 全量回归：**48 通过 · 1 跳过（anysearch 上游波动，设计内）· 0 失败**。

**v4.1-三期 实施记录（2026-09-04 · 已完成）**：
- 3.1 引用诊断（监测⑧卡）：域名榜（probes+台账url聚合，自有绿底，通吃平台/基础层类型标注+策略含义）；四类诊断矩阵（提及未引用/缺席且竞品在场【答案侧】·信源在榜/完全缺席【信源侧】，每类标数据通道）；逐问下钻看探针历史；矩阵一键生成工单。
- 3.2 修复队列 fixQueue：owner/期限/五因子下拉（业务价值×可见度缺口×可修复性×准确性风险÷工作量自动算 P 值并排序）/状态四态/部署后验证（记 verifiedNote+上曲线事件）；冲突合并取并集。
- 3.3 证据库（诊断⑥子页）：引言/案例/荣誉/口径引用 CRUD（引言强制人物+职务），核验流转，≥5条引言进度条；口径红线落 UI（数字事实唯一源=口径表，证据库只存叙述性证据）；一园一档「企业说」自动注入已核验引言。
- 3.4 /api/parse（Ollama JSON 模式抽取提及/倾向/引用域名/竞品共现）+「贴答案自动填」弹窗（竞品候选来自历史共现；解析只填表单不自动提交，人工确认后入库）；模型不可用优雅降级。
- 质量验收：10 样本抽查字段正确率 37/40=92%（≥80% 达标；评分器曾有空集误判 bug 已修；提示词收紧"机构名≠域名"后伪域名消失）。剩余 3 个 miss 均为模糊边界，由人工确认环节兜底。
- 浏览器验收：域名榜 15 行（百科居首，符合真实基线）/矩阵四类全亮/工单 P=200 公式正确/owner+期限落库/引言注入一园一档/贴答案→确认→台账含引用与共现/下钻历史。
- 全量回归：**54 通过 · 1 跳过 · 0 失败**。实施中修 2 个自伤 bug：①证据库子页正则跨界插错 section；②render.evidence 赋值早于 const render（TDZ）——均测试逮住即修。

**v4.1-四期 实施记录（2026-09-04 · 已完成，四期全部完工）**：
- 4.1 组合对比视图：项目卡升级——提及率/vs基线趋势箭头（↑↓→）/台账/最近轮 + 落后项预警（无监测/无口径表/提及率下滑/信源缺席）+ 按趋势降序排序 + 归档（服务器端 archived 标记，数据保留可恢复）；服务器模式用 /api/projects 汇总（跨机器一致），本地缓存即时渲染兜底。
- 4.2 曲线：引擎筛选下拉（11 项，「提及率·答案侧」切为单引擎口径，快照口径与引擎口径分层清晰）；引用份额序列（台账 url 自有域名占比按期）；AI线索副轴（月度业务数据归一化，悬浮看绝对值）。
- 4.3 业务结果（监测⑨卡）：月度表单（AI引荐流量/AI来源线索/备注，同月替换）+ CSV 导出 + 「复制IT字段建议文案」模板（表单加"从哪个AI了解到我们"字段 + GA 口径说明）；与曲线副轴联动。
- 4.4 严重度公式化：矩阵管理「公式打分」四问（买家接近度/竞品替代/准确性风险/营收相关性，各1-5）→ 均值入严重度。
- 4.5 探针 mmx 回退：anysearch 失败/空结果时自动切 mmx（结果标注 source），实测 n=10 可用。
- 浏览器验收：4 项目组合视图（趋势排序 南京↑25pp→成都→青岛↓5pp→蛇口网谷、预警正确含真实"信源缺席"）；引用份额 0%→50% 与种子数据完全吻合；引擎筛选豆包 0%→100% 精确；AI线索副轴+悬浮绝对值；业务月度保存联动曲线；严重度公式默认值→3。
- 全量回归：**60 通过 · 0 跳过 · 0 失败**（本期测试数从 13→60）。
- 至此 V4.1 四期全部交付：多项目资产化 + GEO 发展曲线 + 引用诊断闭环 + 集团规模化。剩余建议（后续迭代）：README 更新 V4 使用指南、鉴权（若对外开放）、快照的服务端自动归档策略。
