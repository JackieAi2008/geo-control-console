# unverified_list.md · 未测与无法验证清单（必填，留空=无效报告）

本次评审按任务书铁律如实列出所有未覆盖、无法验证、证据不完整的点。**回收本报告后，以下各项不应被视为结论。**

## A. 环境与平台层

1. **真机未测**：移动端全部结论来自 375/320 视口模拟（同源 iframe 真实 CSS 宽度 + 程序化测量），未做真机验证——触控目标 ≥44px、拇指区、DPR、字体缩放、安全区、iOS/Android 浏览器差异全部待真机复测。
2. **生产 SSO 登录流程未走**：本地无 cmip SSO 网关（后端默认账号 local），「未登录 302 → 门户登录 → 回跳」链路与登录态过期行为本次不在走查范围。生产版本号经服务器只读探查为 0.1.11，但生产数据库里的存量数据形态（老项目稀疏字段等）与本地全新空库不同。
3. **375 视口直观截图缺失**：截图管线当天两次故障（browser screenshot 超时/capture failed），截图 13、14、18、19 计划项未留存；对应观察均有文字取证（operations_log 对应时间行逐字摘录），但「视觉层」（间距、错位、折叠样式）无图证。
4. **页面 console JS 报错未系统采集**：错误收集器安装后序列化受限，仅能确认「重载后未见收集到错误」，不能证明各页面无 JS 异常。
5. **`?layoutcheck=1` 自检通道本次未产出可见报告**（打开后仅渲染正常页面），其断言结果读取方式未验证，最终以自建 iframe 探针替代。

## B. 功能与数据层

6. **AI 代问真实回答质量未测**：本地未配置豆包通道（只见空态文案），其联网检索、录入台账、判定「提及」的准确性全部未验证。生产有已配通道，需在额度内另行抽测（任务书预算：功能测试 AI 代问提交 ≤15）。
7. **30 问的「答案侧」数据未人工作证**：本次只跑了信源侧（搜索通道自动轮）；「AI 回答里是否真的提及/引用」需真人在各引擎提问核对，AI 无法替代（与监测抽验层同源限制）。
8. **南山大厦演示泄漏的触发机制是行为推断**：实测证实「会话内残留、整页刷新后消失」，但内部实现（演示态挂在哪个状态上、何种操作会清除）未读码证实；「引导中途退出是否也泄漏」未测。
9. **内容评分器评分准确性未验证**：只走查了界面与自带「改写示范（33分→83分）」，自评/机评的一致性、十二条款的判定逻辑未测。
10. **「改写示范」是否随项目个性化未验证**：示范文本内容为蛇口网谷真实化案例，但本次两个测试项目同名（都是蛇口网谷），无法区分是「按项目生成」还是「内置固定示例」。若为固定示例，其他园区项目将看到蛇口网谷的示范文案（同类问题已见于 F1）。
11. **「生成工单」闭环未走完**：诊断矩阵/报告的「生成整改工单 →」点击后的工单页、验收流未走查（时间预算内优先覆盖了任务书三个任务主线）。
12. **设置/归档/备份恢复/操作员署名/知识库内容质量**：仅到「入口可见」级别，未逐项走查。
13. **「已完成地图认领 ✓」自报勾选**：是否影响进度统计/体检分未验证其核验机制。
14. **手机端首登引导未测**：桌面走查已消耗「首次弹出」账号级标记，移动端首登时引导是否弹出、10 步在 375 下的可用性未知。
15. **跨账号/权限**：单一账号走查，多用户协作、署名、数据隔离未测。

## C. 评审者声明

16. **评审者非真实新手**：评审 AI 对系统背景有认知（任务书已声明两法替代：认知走查+大白话审计）。四问作答依据是界面可见 affordance 与文案，但「真实新手会不会困惑」最终以 human_test_kit.md 的真人测试为准。
17. **正面观察不构成验收**：walkthrough.md「做得好的」清单仅为评审记录，不代表回归测试结论；141 条 e2e 之外的改动仍需常规测试流程覆盖。

（本清单共 17 项，覆盖任务书「未测与无法验证清单必填」要求；有内容=有效报告。）
