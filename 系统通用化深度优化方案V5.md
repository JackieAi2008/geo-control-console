# SKIPGEO 通用化深度优化方案 V5

> 版本：v1.0（2026-09-06）
> 输入：①2026-09-06 生产环境专家实测报告（全链路实测+6 项缺陷）②本轮代码级通用性核查
> 定位重申：**本系统是通用 GEO 智控台，不绑定任何特定项目**。蛇口网谷只是第一个种子项目。任何园区、楼宇、商业体、文旅目的地、企业实体进来，都必须能直接用。
> 状态：待确认后实施

---

## 一、背景与问题全景

### 1.1 一个被测试视角掩盖的盲区

2026-09-06 专家实测以蛇口网谷为对象全链路通过——但这恰恰构成盲区：**测蛇口网谷时一切正常的地方，很多正是"只有蛇口网谷才正常"的地方**。例如监测页 30 问全部命中、工单文案语义通顺、物料生成即取即用，都是因为系统里硬编码的就是蛇口的东西。换成任何第二个项目，这些环节会立刻出错。

本轮代码核查把通用性问题全部落到了确切代码行，与专家实测发现的 6 项缺陷合并成下表的完整清单。

### 1.2 问题全景清单

| # | 级别 | 问题 | 确切位置/机制 | 对"任意项目"的后果 |
|---|---|---|---|---|
| G1 | **P0** | **30 问矩阵是蛇口的尺子** | `app.js:16` `defaultState` 中 `prompts: GEO.prompts.map(...)` 无条件拷贝蛇口专属 30 问（P1"招商蛇口产业园区有哪些代表项目"、P3"南海意库和蛇口网谷有什么区别"…）；`app.js:97` `P_prompts()` 兜底同样拷贝 | 新项目跑"30 问监测"，搜的全是蛇口的问题，产出数据对该项目毫无意义——**监测模块整体失效** |
| G2 | **P0** | **工单人话卡的事实描述是蛇口实况** | `data.js:58-68` `GEO.woPlain.E2a.what` 写死"前 10 条全是政府网站、共产党员网……我们自己的官网和公众号一条都没有"；V4.6.1 的 `sub()` 只替换项目名，不替换事实 | 对有官网的项目，工单陈述的是**编造的该项目事实**——违背系统"不编造"的根本原则 |
| G3 | **P0** | **新建项目不收集城市/产业/竞品/运营主体** | `index.html:623-634` 新建只收 名称/形态/域名/品牌词/自有域名 | 物料生成每次手输城市产业（执行页另有一套表单）；对比类问题、竞品参照线、知乎首发 {city} 模板全部缺料；`operator` 默认回退"招商蛇口产业园区（招商产园）" |
| G4 | P1 | parse 答案抽取生产不可用 | `server.py:485-490` `parse_answer` 只走 `pick_chat_model()`（仅 Ollama），未复用 chat 的 `llm_resolve` 三级回退；生产无 Ollama 直接报错且文案让用户 `ollama pull` | 全部用户：30 问人工录入效率工具失效 |
| G5 | P1 | 诊断/执行表单不预填项目数据 | `ops.js:29` 读 `#dgBrand/#dgUrl`，无任何预填；执行页园区名有预填但城市/产业靠 `planInputs` 记忆（`ops.js:343` city 默认"深圳"） | 每次手输；输错品牌词=体检对象错误；非深圳项目漏输城市会拿到"深圳"默认值 |
| G6 | P1 | 双形态文案改造覆盖不全 | `index.html` 执行页"生成6个文件：网站AI可读配置/AI说明文件/结构化数据标签…"静态字符串对 none 项目不准（V4.6.1 只修了工作台） | 无官网项目被误导去要官网文件 |
| G7 | P1 | chat 知识库/Agent 提示词带招商产园背景 | `chat-kb.js:180` "内置……招商产园背景"；多处 FAQ 举例蛇口 | 其他项目用户拿到带别家背景的 Agent 提示词 |
| G8 | P1 | entMode 识别靠 url 推导、不可修正 | 无官网且 url 空 → none（蛇口网谷实测即此，但语义上其承载=招商蛇口官网子页+公众号，更接近 parent）；无手动切换入口 | 形态误判影响体检分母（24/30 项）、工单分支、物料件数 |
| G9 | P1 | 通用性无回归保障 | `test_e2e.py` 全部用蛇口样本（V4.6 曾用万海大厦但核心尺子无第二项目断言） | 通用性缺陷每次迭代都可能回归（本案 G1 即为例证：口径表做了 fresh 分支、问题矩阵漏做） |
| G10 | P2 | own 域名子串匹配偏宽 | `server.py:557` `any(d in u for d in owns)` | 自有域名出现在第三方 URL 路径中会误判命中（低概率但影响北极星指标可信度） |
| G11 | P2 | 渠道卡"首发：首发："前缀重复 | `data.js:397` 抖音卡 `first:"首发：{园区}…"` 模板自带前缀，渲染处再拼一次（CSDN 卡同型） | 观感 bug |
| G12 | P2 | 跨项目文案错位 | 实体体检空品牌词 toast 示例写死"万海大厦"（在蛇口项目里提示另一个项目名） | 观感/困惑 |
| G13 | P2 | 采样引擎优先级、热力矩阵类别写死 | 监测页"豆包、腾讯元宝（人工覆盖最少的引擎优先轮换）"硬编码；类别固定 品牌认知/选址决策/对比竞品/产业服务 | 海外/非园区类对象需要不同引擎组合与类别（如文旅：目的地/攻略） |
| G14 | P2 | 竞品列表无项目配置入口 | 对比类问题与竞品共现（cooccur）依赖竞品名，新项目无竞品配置 | 对比类监测与参照线无料 |

**已确认做对的（不需要动）**：口径表种子已按 fresh 分支隔离（`app.js:15`）；基线发现卡已按 `_isSeedPark` 项目隔离（`app.js:1305`）；台账/快照/曲线按 `doc:project:<id>` 物理隔离；渠道卡主体文案带 {园区}/{city}/{产业} 占位。

### 1.3 根因归纳

五个产品根源，方案按根源设计而不是逐点打补丁：

1. **"尺子"未项目化**（G1/G2/G14）：V4 项目化改造做的是"容器"（项目库/隔离存储/切换器），没做"内容尺子"（问题矩阵/工单事实/竞品）。种子项目的尺子被无条件继承。
2. **事实与模板未分层**（G2/G6/G11）：文案里混着"通用动作指引"与"蛇口特定事实"两种东西，替换机制只到名字层。
3. **LLM 能力轨道不统一**（G4）：chat 有三级回退，parse 只有一条轨道——同一系统两套 LLM 接入标准。
4. **项目上下文传递断裂**（G3/G5/G8/G12）：项目元数据字段不够（城市/产业/竞品缺位），已有的字段也不回流到表单（不预填）。
5. **通用性无回归防线**（G9）：测试样本单一，"第二项目能不能用"从来不是断言。

---

## 二、设计原则（四条铁律）

1. **尺子随项目走**：问题矩阵、口径骨架、竞品列表、工单文案、渠道首发，全部从项目元数据生成或来自该项目实时数据；全局常量里不允许存在任何具体项目名。
2. **事实不编造**：模板文案只写项目无关的动作指引；一切项目相关的事实陈述（"我们的官网现在……"）必须来自该项目的真实诊断/监测数据，没有数据就明说"尚未体检，先跑一次"。
3. **冷启动 ≤2 分钟**：新项目从点"新建"到能跑第一次实体体检+拿到第一版物料，输入不超过 7 个字段、点击不超过 3 次。
4. **降级诚实且大白话**：任何依赖外部能力的功能（LLM/搜索通道）失效时，提示必须说清楚"现在不能用的原因+你还能怎么办"，禁止出现让业务人员去服务器执行命令的文案。

---

## 三、分期方案

### 一期：尺子项目化（通用性生死线，G1/G2/G3/G4）

#### 1.1 新建项目向导增强（G3，先做——它是后面所有生成的数据源）

**现状**：新建收集 5 字段（名称/形态三选一/承载页域名/品牌词/自有域名）。

**方案**：扩展为 9 字段，一次收齐生成尺子所需的全部参数：

| 字段 | 必填 | 用途 |
|---|---|---|
| 项目名称 | ✓ | 全局显示 |
| 网上哪里能找到（形态三选一） | ✓ | 体检路线/分母/物料分支 |
| 官方承载页域名 | 形态≠none 必填 | 一键诊断 |
| 品牌词 | ✓ | 品牌词搜索/实体体检对象 |
| 自有域名（多行） | 建议 | own 判定/引用份额 |
| **城市/片区**（新增） | ✓ | 选址类问题生成、渠道首发 {city} |
| **主导产业**（新增，逗号分隔） | ✓ | 产业类问题生成、物料生成 |
| **主要竞品**（新增，顿号分隔 2-5 个） | 建议 | 对比类问题生成、竞品参照线 |
| **运营主体全称**（新增） | 建议 | 物料署名/企查查核对指引（替代默认回退"招商蛇口产业园区"） |

- 自有域名输入框下方给建议行："常见自有阵地：官方公众号 mp.weixin.qq.com（有就填）"。
- 城市输入带 placeholder"如：深圳南山"；竞品 placeholder"如：深圳湾科技园、天安数码城"。
- 全部字段存入项目元数据（projects API + `doc:project:<id>`），后续可在"项目设置"里修改。
- **验收**：新建一个虚构项目（例：南京金融城），9 字段一次收齐；`projCtx()` 能取到 city/industry/competitors/operator。

#### 1.2 问题矩阵模板引擎（G1，本方案核心）

**现状**：`defaultState` 与 `P_prompts()` 无条件拷贝 `GEO.prompts`（蛇口 30 问）。

**方案**：三步走——

**a) 模板化**：把 30 问改写成带参数的问法模板库 `GEO.promptTemplates`（每条：id/cat/severity/模板/所需参数），例：

| id | cat | 模板 |
|---|---|---|
| P1 | 品牌认知 | `{operator}旗下有哪些代表性园区/项目？` |
| P2 | 品牌认知 | `{brand}是什么？主要有什么类型的入驻企业？` |
| P3 | 品牌认知 | `{competitor1}和{brand}有什么区别？` |
| P7 | 选址决策 | `{city}科技企业选址有哪些园区推荐？` |
| P13 | 选址决策 | `{region}生物医药园区怎么选？（若主导产业含生命科学）` |
| P19 | 对比竞品 | `{city}知名产业园区有哪些？各自什么特点？` |
| P20 | 对比竞品 | `{competitor1}、{competitor2}、{brand}的对比？` |

- 条件渲染：产业相关的问题（P13 类）仅当该产业在"主导产业"里时实例化；竞品问题仅当配置了竞品；未满足条件的模板跳过并从未用模板池补位，保证每档总数固定。
- **两档模板**（承接 V4.6 预告的"小对象与 30 问结构错配"）：
  - **园区版 30 问**（4 类别配比 品牌认知6/选址12/对比5/产业服务7，severity 种子沿用）；
  - **轻量版 12 问**（品牌3/选址5/对比2/产业2）——楼宇、单体项目默认此档，新建向导里按形态/规模二选一，可随时切换（切换=重新实例化，已录台账按 promptId 保留）。
- 兼容机制：实例化后的 questions 与现有 `state.prompts` 结构完全一致（id/cat/q/severity），"管理问题矩阵"手动编辑、台账、热力矩阵、采样逻辑零改动。

**b) 触发时机**：`defaultState` 与 `P_prompts()` 的种子逻辑改为"若项目元数据齐备→模板实例化；缺参→先给待补提示（不生成半成品问题）"。蛇口网谷（p_default）`state.prompts` 已有存量，**不迁移不动**——它现在的问题本来就对它是对的。

**c) 参数缺口兜底**：模板某参数缺失（如未填竞品）时，该问题以"【待补：在项目设置补充竞品后自动生成】"占位显示在管理矩阵里，不进入监测跑批。

- **验收**：①新建"南京金融城"（城市南京/产业金融科技/竞品德基广场、南京国际金融中心），30 问全部实例化且含正确品牌词/城市/竞品，**零蛇口/深圳/招商字样**；②该矩阵跑一轮 30 问正常入台账；③蛇口网谷 30 问与现状逐字一致；④切轻量版 12 问正常、切回正常。

#### 1.3 工单人话卡事实层项目化（G2）

**现状**：`sub()` 只替换项目名；`what` 里"官网和公众号一条都没有"等是蛇口实况。

**方案**：woPlain 每条拆成两层——

- **动作层**（通用，保留模板）：who/steps/how 里项目无关的指引。
- **事实层**（数据驱动）：`what` 不再写死，改为运行时用该项目实时数据拼装的小函数。以 E2a 为例：
  - 若该项目跑过实体体检/品牌词搜索：`what = "搜索「{brand}」前10信源是：{top3域名}，自有渠道{命中数}条——{判定}"`（数据来自 `state.lastDiag`/最近 probes）；
  - 若没跑过：`what = "本项目还没有跑过品牌词体检，先到「诊断→实体体检」跑一次（约10秒），这里会自动写明实际情况。"`
- 实施方式：`woCard`/`workOrderMd` 渲染时对事实层字段走 `buildWhat(id, ctx)`，ctx 含项目实时诊断快照；`sub()` 机制保留负责名字层。
- **验收**：①蛇口网谷工单 what 显示其实测事实（前10第三方域名）；②新建空项目工单 what 显示"尚未体检"引导；③任意项目工单全文无蛇口事实陈述。

#### 1.4 parse 生产可用（G4，上轮 P1，半小时级）

**方案**：`parse_answer` 的模型调用改走 `llm_resolve` 三级回退（个人配置>系统 env>Ollama），与 `_chat` 一致：src=user/server 走 `llm_remote_open`（DeepSeek 等对 JSON 输出的兼容处理：解析失败时沿用现有正则兜底），src=local 走 Ollama `format:"json"`；none 时报错文案改大白话："当前没有可用的大模型。两种解决方式：①点对话窗口右上角 ⚙ 配置你自己的模型（推荐，一次配置全系统可用）；②联系管理员在服务器设置。"
- 顺带把"贴答案自动填（本机AI解析）"按钮文案的"（本机AI解析）"删掉（生产/远程模型下该说法不对）。
- **验收**：生产 admin（DeepSeek 个人配置）贴一段含品牌+竞品的 AI 回答，正确抽出 mention/sentiment/竞品共现；未配 LLM 的新用户看到大白话指引而非 `ollama pull`。

**一期合计约 2.5 天。一期完成即达成："任何项目进来，监测、工单、录入效率三个核心模块立即正确"。**

---

### 二期：上下文贯通与一致性（G5/G6/G7/G8/G9/G10）

#### 2.1 全表单预填体系（G5）

- 诊断页：进入时 `#dgBrand` 预填项目品牌词、`#dgUrl` 预填承载页域名（有则填，无则空）；表单值与项目元数据双向同步（改了域名保存时回写项目设置并留痕"更新项目信息"）。
- 执行页：园区名/城市/主导产业全部从 `projCtx()` 预填，手动改动记入 `planInputs`（本项目记忆），下次默认带出；`ops.js:343` 的 `|| "深圳"` 默认值删除——缺城市就要求补，不许默认。
- 选题单：园区下拉已联动，主导产业输入框预填。
- 空品牌词校验提示的示例项目名改为当前项目语境："请先填写品牌词（如：{当前项目名}）"（G12 一并解决）。
- **验收**：蛇口网谷进诊断页品牌词自动带出；南京金融城进执行页三字段全预填；改城市后重进保留。

#### 2.2 双形态文案全覆盖审计（G6）

- 机制化而非逐条补：新建 `renderByMode(siteText, parentText, noneText)` 工具函数，把全站涉及"文件件数/官网动作"的静态文案改为三态渲染；本次扫描清单至少包括：工作台三步向导②（已对，复核）、**执行页生成说明**、诊断报告导出说明、chat-kb 相关 FAQ、90 天方案里的官网周任务。
- 验收方式：e2e 增加"三种形态项目各走一遍文案断言"（无官网项目全站不出现"传到官网根目录"类指引）。

#### 2.3 chat 知识库与 Agent 提示词去绑定（G7）

- `chat-kb.js` FAQ 的蛇口举例改为中性示例或 `{当前项目名}` 动态注入；"内置招商产园背景"的 Agent 提示词改为按当前项目注入背景（品牌词/城市/产业/运营主体）。
- **验收**：切到第二项目后问"帮我生成 Agent 提示词"，产出背景为该项目而非招商产园。

#### 2.4 entMode 识别增强与手动切换（G8）

- 项目设置卡新增"承载形态"三选一（随时可改），当前值高亮；改动留痕并触发分母/物料分支重算提示。
- 智能提示：新建/设置时若填入的域名 host 与项目自身品牌域不一致（如填了 cmsk1979.com 而项目名是蛇口网谷），toast 提示"这是上级单位官网？若项目无独立官网，建议选「挂在上级单位官网」"。
- 顺手校正蛇口网谷：parkUrl 补 www.cmsk1979.com、形态改 parent（恢复官网体检双入口，30 项分母）。
- **验收**：三态切换后体检分母、物料件数、工单分支全部随之正确变化。

#### 2.5 own 判定健壮化（G10）

- `server.py run_probe/_probe_mmx` 与前端 hostOf 判定统一改为：`host === d || host.endsWith("." + d)`（hostname 后缀匹配）；同步加一条 e2e：own=["cmhk.com"] 时 www.cmhk.com 命中、fake-cmhk.com.evil.com 不命中。
- **验收**：该 e2e 通过；蛇口项目复跑 30 问命中数与现状一致（3 问）。

#### 2.6 通用性回归防线（G9，本期的"保险丝"）

- `test_e2e.py` 新增第二项目全套用例（虚构"南京金融城"，避免与真实数据耦合）：新建→9 字段→30 问模板断言（含品牌词/城市/竞品，**断言全文不含"蛇口|招商|深圳"**）→实体体检→物料断言（5 件、无 cmsk 域名）→工单断言（what 为数据驱动或"尚未体检"）→chat 状态感知断言。
- 该套例成为后续所有迭代的必跑项（与现有 84 项并列，预计新增 15-20 项）。
- **验收**：全量 e2e ≥100 项全绿。

**二期合计约 2 天。**

---

### 三期：打磨与增值（G11/G13/G14 及体验增强）

1. **小 bug 清理**（G11）：渠道卡 `first` 模板统一去掉自带前缀，渲染处单一出口拼"首发："；CSDN/抖音/百科三卡复验。
2. **竞品参照配置化**（G14）：项目设置的竞品列表接入曲线"竞品同时出现率·参照"线与对比类问题生成；竞品空时该线隐藏并提示"在项目设置补充竞品后启用"。
3. **引擎组合按项目配置**（G13）：项目设置新增"重点引擎"多选（默认六引擎+搜索通道；海外型项目可勾 ChatGPT/Perplexity 进采样优先组），热力矩阵行按所选引擎渲染；类别标签允许项目级改名（文旅项目把"选址决策"改"目的地攻略"）。
4. **项目模板库**（增值）：预设"产业园/楼宇办公/商业综合体/文旅目的地"四类模板（问题矩阵配比+口径字段集+类别标签差异），新建时可选从模板起歩——把 1.2 的两档扩展成按对象类型起步。
5. **组合视图口径标注强化**：项目库卡片已带"24项/30项"口径 title，三期在组合排序（按体检分）时加"跨形态仅同形态可比"的显式提示条。

**三期合计约 2 天。**

---

## 四、实施计划

| 期 | 内容 | 工期 | 依赖 | 主要风险与对策 |
|---|---|---|---|---|
| 一期 | 尺子项目化（向导增强→模板引擎→工单事实层→parse） | 2.5 天 | 无 | 模板实例化的条件渲染规则复杂度 → 先固化园区版 30 问模板表，轻量版延后到一期末；蛇口存量数据不动，迁移风险为零 |
| 二期 | 上下文贯通（预填/双态审计/chat 去绑定/entMode/own/e2e 防线） | 2 天 | 一期 1.1 的字段 | 双态审计面广 → 用 renderByMode 机制化+e2e 三态断言兜住 |
| 三期 | 打磨增值 | 2 天 | 一二期 | 引擎/类别配置引入状态复杂度 → 默认值与现状完全一致，配置是加法不是改动 |

发版节奏：每期独立发版（rsync 三步+全量 e2e），一期上线后即可邀第二个真实项目试用验证。

## 五、验收标准汇总（可勾选）

- [ ] 新建任意虚构项目（含 9 字段）2 分钟内完成，落地即可跑实体体检
- [ ] 该项目 30 问（或 12 问）全部实例化，全文无"蛇口|招商|深圳"残留
- [ ] 该项目工单/物料/对话助手的全部事实陈述来自其自身数据，无数据时明示"尚未体检"
- [ ] 生产贴答案自动填可用（DeepSeek 轨道），失败提示为大白话
- [ ] 蛇口网谷全部存量数据、30 问、口径表逐字不变
- [ ] 全部表单从项目元数据预填；无"深圳"硬默认
- [ ] own 判定后缀匹配 e2e 通过
- [ ] 全量 e2e（含第二项目套例）≥100 项全绿
- [ ] 双端（1440/375）截图复验通过

## 六、明确不做（本期 out of scope）

- 多租户权限/组织架构（当前单团队使用，SSO 已够）
- 答案侧自动采集（六引擎无公开 API，行业共性；维持分层采样+批量粘贴+parse 辅助路线）
- 知识库引擎规则的自动更新（维持人工季度评审）
- 国际化界面（中文单语言）

---

*本方案与《GEO实操方案V2》《系统升级实施计划V4》的关系：V2 是方法论总纲，V4 解决了多项目容器，本方案（V5）解决多项目尺子——三层叠加后系统才真正是"任何项目进来都能用"的通用平台。*
